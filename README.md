# ☢️ PyvnytsyaBot (Пивниця)

Telegram бот для гри в "Бункер" з інтеграцією AI (Gemini).

## 🛠 Технології
- Python 3.10+
- aiogram 3.x
- PostgreSQL + SQLAlchemy (asyncpg)
- Google Gemini API

## 🚀 Встановлення

1. **Клонуйте репозиторій** (або використовуйте поточну папку).

2. **Створіть віртуальне середовище:**
   ```bash
   python -m venv .venv
   .venv\Scripts\activate  # Windows
   source .venv/bin/activate # Linux/macOS
   ```

3. **Встановіть залежності:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Налаштуйте змінні середовища:**
   - Скопіюйте `.env.example` в `.env`.
   - Відкрийте `.env` і заповніть:
     - `BOT_TOKEN`: Токен вашого бота від @BotFather.
     - `GEMINI_API_KEY`: API ключ від Google AI Studio.
     - `DB_*`: Налаштування вашої бази даних PostgreSQL.

5. **Запустіть базу даних:**
   Переконайтеся, що у вас встановлений PostgreSQL і створена база даних з ім'ям, вказаним в `.env` (за замовчуванням `pyvnytsya_db`).

6. **Запустіть бота:**
   ```bash
   python main.py
   ```

## 🎮 Як грати
1. Напишіть `/start` боту.
2. Створіть кімнату через меню.
3. Надішліть код друзям.
4. Коли всі приєднаються, натисніть "Почати гру".
5. Бот згенерує сценарій та роздасть характеристики.
